const initState = {
  resetPasswordStatus: 0,
  responseRegister: {},
  resLogin: {},
  account: null,
  isVerifyCode: false,
  token: null,
  accountsRender: null,
  accountSelect: {},
};

const authState = {
  initState,
};

export default authState;
