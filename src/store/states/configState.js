const initState = {
  countryCode: null,
  languageCode: 'en',
  callingCode: null,
  longitude: null,
  latitude: null,
  dataConfig: {},
};

const configState = {
  initState,
};

export default configState;
